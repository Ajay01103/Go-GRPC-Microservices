GOOSE_DRIVER=postgres
GOOSE_DBSTRING=postgres://postgres:postgres@localhost:5433/authdb?sslmode=disable
GOOSE_MIGRATION_DIR=db/migrations

# Full DSN used by the auth service directly
AUTH_DB_URL=postgres://postgres:postgres@localhost:5433/authdb?sslmode=disable

# ─── Redis ────────────────────────────────────────────────
REDIS_PASSWORD=redispass
REDIS_PORT=6379
REDIS_URL=redis://:redispass@localhost:6379/0

# ─── Auth Service ─────────────────────────────────────────
AUTH_GRPC_PORT=50051

# ─── JWT & JWKS ───────────────────────────────────────────
# Must be at least 32 characters for HS256 (legacy, not used with RS256)
JWT_SECRET=supersecretjwtkey_changethis

# JWKS endpoint served at: http://localhost:50051/.well-known/jwks.json
# Auth service uses RSA asymmetric signing (RS256)
# Public keys automatically rotated and available for other services

# Go duration strings: 15m, 1h, 168h etc.
ACCESS_TOKEN_DURATION=15m
REFRESH_TOKEN_DURATION=168h
# Keep signing keys longer than refresh tokens so restart does not invalidate
# still-valid tokens signed by older keys.
RSA_SIGNING_KEY_RETENTION_DURATION=336h
